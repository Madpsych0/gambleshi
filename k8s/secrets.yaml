# =============================================================================
# Application Secrets — TEMPLATE
# =============================================================================
# WARNING: This is a TEMPLATE file. Do NOT commit real secret values to Git.
#
# To create secrets on the cluster, use:
#   oc create secret generic gambleshi-secrets \
#     --from-literal=app-secret-key='YOUR_SECRET_HERE' \
#     -n navaneethfr-dev
#
# Or for image pull secrets:
#   oc create secret docker-registry gambleshi-pull-secret \
#     --docker-server=quay.io \
#     --docker-username=YOUR_USERNAME \
#     --docker-password=YOUR_PASSWORD \
#     -n navaneethfr-dev
#   oc secrets link gambleshi-sa gambleshi-pull-secret --for=pull
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: gambleshi-secrets
  namespace: navaneethfr-dev
  labels:
    app.kubernetes.io/name: gambleshi
    app.kubernetes.io/component: frontend
    app.kubernetes.io/part-of: gambleshi
type: Opaque
data:
  # Base64-encoded placeholder — replace with real values via `oc create secret`
  # echo -n 'change-me-in-production' | base64
  app-secret-key: Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24=
